<UserControl x:Class="RHTools.ReplaceToolRuleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RHTools"
             Width="226">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </UserControl.Resources>
    <Border BorderThickness="8,1,3,1" BorderBrush="Green">
        <StackPanel>
            <TextBlock Text="FROM"/>
            <TextBox Name="TextBoxFrom" Foreground="DarkGreen" Text=""/>
            <TextBlock Text="TO"/>
            <TextBox Name="TextBoxTo" Foreground="DarkRed" Text=""/>
        </StackPanel>
    </Border>
</UserControl>
